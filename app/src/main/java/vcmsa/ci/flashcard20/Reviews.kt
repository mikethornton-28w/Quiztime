package vcmsa.ci.flashcard20

import android.content.Intent
import android.os.Bundle
import android.widget.Button
import android.widget.TextView
import androidx.activity.enableEdgeToEdge
import androidx.appcompat.app.AppCompatActivity


class Reviews:AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?){
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContentView(R.layout.activity_reviews)

        val score = intent.getIntExtra("score", 0)



        val scoreText: TextView = findViewById(R.id.scoreText)

        val reviewButton: Button = findViewById(R.id.reviewButton)



        val feedback = if (score >= 2)"Congratulations" else "So close. Keep Practising"
        scoreText.text = "You got $score/4. $feedback"


































































    }
























































































































































































































}